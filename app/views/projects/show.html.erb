
<h1>Project: <%= @project.name %></h1>

This project has two branches.  You can import them all using this directive:

<pre>
deb http://debian.example.com/ <%= @project.name.downcase %> <%= @project.branches.map {|b| b.name.downcase }.join(' ') %>
</pre>

<h2>Branches</h2>

<%= form_for(Branch.new) do |f| %>
  <p><%= f.text_field :name, placeholder: "Branch Name" %> <%= f.submit %></p>
  <%= f.hidden_field :project_id, value: @project.id %>
<% end %>

<%= render partial: 'branches/table', locals: { branches: @project.branches } %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
